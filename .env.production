# Used by `vite build` (production mode). Overrides .env so local dev URLs are not baked into dist.
# See src/lib/config.js — VITE_* is embedded at build time; set VITE_WP_GRAPHQL_URL on Vercel too.
VITE_WP_GRAPHQL_URL=https://backend.shadrach-tuck.dev/graphql
